apiVersion: v1
kind: Service
metadata:
  labels:
    name: inventory-service
  name: inventory-service
  namespace: microservice
spec:
  selector:
    app: microservice-demo
    name: inventoryService
  ports:
  - protocol: TCP
    port: 6500
    targetPort: http   